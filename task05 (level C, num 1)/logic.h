#pragma once
#include <iostream>
#include <string>
#include <cmath>

#define SIZE 20

using namespace std;

double calculate_geo_mean(int arr[SIZE], int length);
int cout_elements(int arr[SIZE], int length);